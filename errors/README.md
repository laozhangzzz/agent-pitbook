# Error Book

`errors/` stores recurring failures in Agent Pitbook itself.

This is inspired by later LLM-Wiki work that treats structural and semantic errors as persistent correction targets.

Examples:

- generated pit lacks source evidence
- duplicate pit created instead of updating canonical record
- stale workaround remains marked verified
- adapter suggests unsafe command
- search misses a known pit
- claim contradicts a newer source

